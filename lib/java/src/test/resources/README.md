# Updating `sample.parquet` file

In order to keep the `sample.parquet` file up to date, you can run the following command:

```bash
 csv2parquet src/test/resources/sample.csv -o src/test/resources/sample.parquet
```
