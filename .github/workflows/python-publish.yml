# Build Python packages (openlinktoken & openlinktoken-pyspark) and attach artifacts to release.
# NOTE: PyPI publication intentionally disabled until readiness confirmed.
# Triggers mirror the Maven publish workflow: on release creation/publish or completion of release workflow.

name: Python Package

on:
  release:
    types: [created, published]
  workflow_run:
    workflows: ["Create Release on Merge"]
    types: [completed]
  workflow_dispatch:
    inputs:
      version:
        description: "Version to publish (e.g., 1.12.5)"
        required: true
        type: string

permissions:
  contents: read

jobs:
  release-info:
    uses: ./.github/workflows/release-context.yml
    secrets: inherit
    with:
      lookup-release: true
      # Use manual input version if provided (add v prefix for tag format)
      release-tag: ${{ github.event.inputs.version && format('v{0}', github.event.inputs.version) || '' }}

  build_and_test:
    needs: release-info
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      VERSION: ${{ needs.release-info.outputs['version'] }}
      TAG: ${{ needs.release-info.outputs['tag'] }}
      RELEASE_FOUND: ${{ needs.release-info.outputs['release-found'] }}
      RELEASE_ID: ${{ needs.release-info.outputs['release-id'] }}

    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ needs.release-info.outputs['release-found'] == 'true' && needs.release-info.outputs.tag || github.sha }}

      - name: Setup toolchain (Python only)
        uses: ./.github/actions/setup-toolchain
        with:
          enable-java: false
          enable-python: true
          python-version: "3.10"

      - name: Install dependencies and run tests
        run: |
          # Install core package in editable mode for testing
          uv pip install --break-system-packages --system -e lib/python/openlinktoken/
          # Install core-ai in editable mode for its focused tests
          uv pip install --break-system-packages --system -e lib/python/openlinktoken-core-ai/
          # Install requirements for everything including testing tools
          uv pip install --break-system-packages --system -r lib/python/openlinktoken/requirements.txt \
            pytest pytest-cov ruff

          # Run tests for core package
          cd lib/python/openlinktoken
          PYTHONPATH=src/main pytest src/test --junitxml=../test-results.xml --cov=src/main --cov-report=xml:coverage.xml

          # Run tests for core-ai without invoking the large model
          cd ../openlinktoken-core-ai
          PYTHONPATH=src/main:../openlinktoken/src/main pytest src/test

      - name: Build distributions
        run: |
          (cd lib/python/openlinktoken && uv build -o dist/)
          (cd lib/python/openlinktoken-core-ai && uv build -o dist/)
          (cd lib/python/openlinktoken-pyspark && uv build -o dist/)

      - name: Upload Python artifacts
        uses: actions/upload-artifact@v7
        with:
          name: python-packages-${{ env.TAG }}
          path: |
            lib/python/openlinktoken/dist/*.whl
            lib/python/openlinktoken/dist/*.tar.gz
            lib/python/openlinktoken-core-ai/dist/*.whl
            lib/python/openlinktoken-core-ai/dist/*.tar.gz
            lib/python/openlinktoken-pyspark/dist/*.whl
            lib/python/openlinktoken-pyspark/dist/*.tar.gz

  publish_to_pypi:
    needs: [release-info, build_and_test]
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write # required for PyPI Trusted Publishing (OIDC) — no API token needed
      contents: read
    steps:
      - name: Download Python artifacts
        uses: actions/download-artifact@v8
        with:
          name: python-packages-${{ needs.release-info.outputs['tag'] }}
          path: dist_temp

      - name: Prepare Python distributions
        run: |
          mkdir -p dist
          find dist_temp -type f \( -name '*.whl' -o -name '*.tar.gz' \) -exec cp {} dist/ \;

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist
          skip-existing: true
